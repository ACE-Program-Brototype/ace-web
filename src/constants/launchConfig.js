/**
 * Launch Intro Configuration
 *
 * Set `IS_LAUNCH_ENABLED` to `true` to enable the inauguration launch intro video.
 * Set to `false` to disable the launch video completely and load the site directly.
 */
export const IS_LAUNCH_ENABLED = true;
